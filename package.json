{
  "name": "finlit-expense-tracker",
  "version": "1.0.0",
  "description": "Student Budget & Expense Tracker - Full Stack Web App",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "keywords": [
    "expense-tracker",
    "budget",
    "mern-stack",
    "react",
    "nodejs"
  ],
  "author": "FinLit Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
